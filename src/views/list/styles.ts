import styled from "styled-components";

export const RegionHeading = styled.h2`
  font-weight: normal;
  font-size: 0.8rem;
  text-transform: uppercase;
  color: #0088ff;
`;

export const ListContainer = styled.div`
  padding: 1rem 0 1rem 1rem;
`;
